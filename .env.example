# ==============================================================================
# 🌍 GLOBAL AYARLAR
# ==============================================================================
ENV="development"
# Log Level: DEBUG (Geliştirme) veya INFO (Prodüksiyon)
RUST_LOG="debug,sentiric_tts_gateway_service=trace"
SERVICE_VERSION="0.1.0"
ENV_FILE_PATH=.env

# --- Dosya Yolları ---
CONFIG_REPO_PATH=../sentiric-config
CERTIFICATES_REPO_PATH=../sentiric-certificates

# --- Güvenlik (mTLS) ---
GRPC_TLS_CA_PATH=/sentiric-certificates/certs/ca.crt

# ==============================================================================
# 🆔 PROJE KİMLİĞİ VE AĞ
# ==============================================================================
PROJECT_NAME="sentiric"
PROJECT_TLD="cloud"
NETWORK_NAME="sentiric.cloud"
NETWORK_SUBNET=10.88.0.0/16
NETWORK_GATEWAY=10.88.0.1

# ==============================================================================
# 🗣️ TTS GATEWAY SERVICE KONFİGÜRASYONU
# ==============================================================================

# --- Servis Adresleme (Docker İçi) ---
# IP Bloğu: 10.88.40.1 => Port Bloğu: 1401X
TTS_GATEWAY_SERVICE_HOST=tts-gateway-service
TTS_GATEWAY_SERVICE_IPV4_ADDRESS=10.88.40.1
TTS_GATEWAY_SERVICE_HTTP_PORT=14010
TTS_GATEWAY_SERVICE_GRPC_PORT=14011
TTS_GATEWAY_SERVICE_METRICS_PORT=14012

# --- Servis Sertifikaları ---
TTS_GATEWAY_SERVICE_CERT_PATH=/sentiric-certificates/certs/tts-gateway-service-chain.crt
TTS_GATEWAY_SERVICE_KEY_PATH=/sentiric-certificates/certs/tts-gateway-service.key

# --- Cache (Redis) ---
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_URL=redis://redis:6379

# --- Upstream Services (Uzman Motorlar) ---
# Gerçek ortamda bu adresler diğer konteynerleri işaret eder.
# Geliştirme ortamında mock servislere yönlendirilebilir.
TTS_EDGE_SERVICE_TARGET_HTTP_URL=http://tts-edge-service:14020
TTS_COQUI_SERVICE_TARGET_HTTP_URL=http://tts-coqui-service:14030

# --- Test / Mock Ayarları ---
# Geliştirme sırasında gerçek GPU'lu motorlar yerine mock kullanmak için
# TTS_EDGE_SERVICE_TARGET_HTTP_URL=http://mock-tts-engine:80